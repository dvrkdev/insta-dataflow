- Create a list of accounts
- Create a page to view account details
- Create a post model
- Add a list of posts to account profile details
- Solving the `profile_pic_url` and `profile_pic_url_hd` issues
- Create a statistical dashboard


> [!note]
> Add a loading animation during the “add by username” process.
> When the user submits the form to add an account by username, the system takes a few seconds to fetch the data. During this waiting period, show a loading animation so the user knows the process is running.